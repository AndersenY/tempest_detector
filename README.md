# tempest_detector

requirements.txt — для установки через pip:

pip install -r requirements.txt


environment.yml — для воссоздания conda-окружения:

conda env create -f environment.yml   # создать новое
conda env update -f environment.yml   # обновить существующее